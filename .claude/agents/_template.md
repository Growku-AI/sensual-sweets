---
name: my-agent
description: >
  What this specialist does AND when to delegate to it. Begin trigger phrases
  with "Use proactively when…". Be specific — vague descriptions never get picked.
tools: Read, Grep, Glob
model: inherit
---

You are <role>. When invoked:

1. …
2. …

Return <output format>. Constraints: <what you must not do, e.g. "do not modify files">.
